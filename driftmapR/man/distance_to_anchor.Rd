% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/movement.R
\name{distance_to_anchor}
\alias{distance_to_anchor}
\title{Measure aligned distance to an entity or fixed coordinate anchor}
\usage{
distance_to_anchor(object, anchor)
}
\arguments{
\item{object}{An aligned \code{driftmap}.}

\item{anchor}{A character entity ID present in every period, or a finite numeric
vector \code{c(x, y)} in the aligned reference coordinate system.}
}
\value{
A tidy data frame with \code{entity}, \code{time}, \code{x}, \code{y}, \code{anchor_x}, \code{anchor_y},
and \code{distance}. The entity anchor's own distance is zero in every period.
}
\description{
Measure aligned distance to an entity or fixed coordinate anchor
}
\details{
This is a distance metric, distinct from the \code{anchors} used for
alignment. An entity anchor may itself move. No distance change or uncertainty
is inferred automatically; compare adjacent periods for the same entity.
}
\examples{
d <- data.frame(entity = rep(letters[1:4], 2), time = rep(1:2, each = 4),
                x = rep(c(0, 2, 0, 1), 2), y = rep(c(0, 0, 2, 1), 2))
fit <- drift_data(d) |> align_snapshots()
distance_to_anchor(fit, "a")
}
