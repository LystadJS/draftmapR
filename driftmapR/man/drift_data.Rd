% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{drift_data}
\alias{drift_data}
\title{Construct a longitudinal coordinate-map object}
\usage{
drift_data(
  data,
  periods = NULL,
  coords = c("x", "y"),
  original_data = NULL,
  embedding_metadata = list()
)
}
\arguments{
\item{data}{A data frame with \code{entity}, \code{time}, and two coordinate columns.
Additional columns (for example, supplied cluster labels) are retained.}

\item{periods}{Optional complete, ordered vector of expected time values.
Every declared period must be observed. Numeric, Date, and POSIXct times
are sorted by default; ordered factors use their full level order.
Character times and unordered factors require explicit \code{periods}.}

\item{coords}{Character vector of exactly two coordinate column names.}

\item{original_data}{Optional original data retained without interpretation.}

\item{embedding_metadata}{List of caller-supplied embedding metadata.}
}
\value{
An S3 \code{driftmap} list. \code{coordinates} holds canonical \code{entity}, \code{time},
\code{x}, \code{y}, and \code{period_index} columns. \code{aligned} and \code{transformations} are
populated by \code{\link[=align_snapshots]{align_snapshots()}}. \code{\link[=match_clusters]{match_clusters()}} populates \code{clusters}.
\code{movement} and \code{bootstrap} are reserved slots, initially \code{NULL}; movement
functions return tables without mutating the object. \code{diagnostics} and
\code{settings} record the run.
}
\description{
Construct a longitudinal coordinate-map object
}
\details{
Only two-dimensional maps are supported in this prototype. Missing
entity observations are allowed, but missing/nonfinite coordinates are not.
Without an explicit schedule, numeric gaps cannot identify a missing period:
times 1 and 3 are simply two observed snapshots. Supply \code{periods = 1:3} to
detect an entirely missing period 2. No times or coordinates are imputed.
}
\examples{
d <- data.frame(entity = rep(letters[1:4], 2), time = rep(1:2, each = 4),
                x = rep(c(0, 2, 0, 1), 2), y = rep(c(0, 0, 2, 1), 2))
object <- drift_data(d)
print(object)
}
