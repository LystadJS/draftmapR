% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedding.R
\name{embed_snapshots}
\alias{embed_snapshots}
\title{Embed repeated feature data or dissimilarities in two dimensions}
\usage{
embed_snapshots(
  data,
  features = NULL,
  method = c("pca", "cmds"),
  periods = NULL,
  standardize = c("none", "first", "pooled", "period"),
  feature_weights = NULL,
  negative_eigen = c("error", "truncate"),
  eigen_tol = 1e-10
)
}
\arguments{
\item{data}{A long data frame containing \code{entity}, \code{time}, and explicitly
selected numeric features. For \code{method = "cmds"}, alternatively a list of
labeled \code{dist} objects or square dissimilarity matrices, one per period.}

\item{features}{Names of at least two distinct feature columns. Required for
data-frame input; never inferred from numeric metadata.}

\item{method}{\code{"pca"} for centered SVD scores or \code{"cmds"} for classical MDS.}

\item{periods}{Ordered schedule, as in \code{\link[=drift_data]{drift_data()}}. For distance lists,
supply one value per element, or use fully named lists whose names define
the period order. If both are supplied, names must agree with the schedule.}

\item{standardize}{Standard-deviation reference: \code{"none"} (common original
units), \code{"first"}, \code{"pooled"} (all entity-time rows, observation weighted),
or \code{"period"}. All feature maps are centered within period. Period-specific
scaling can remove substantive changes in feature dispersion.}

\item{feature_weights}{Optional finite nonnegative feature weights, applied
through their square roots. Unnamed weights follow \code{features}; named weights
must match them exactly. Weights are not normalized. Integer weights reproduce
column multiplicities, including omitted columns with zero weight. These
weights alone do not define a bootstrap or uncertainty estimate.}

\item{negative_eigen}{For classical MDS, \code{"error"} rejects materially negative
Gram eigenvalues; \code{"truncate"} warns and keeps the leading two positive
eigenvalues. No additive correction is implemented.}

\item{eigen_tol}{Relative spectral tolerance strictly between zero and one.
Two retained eigenvalues must exceed this fraction of the largest absolute
eigenvalue. A second/third eigenvalue tie at this tolerance warns.}
}
\value{
A new, unaligned \code{driftmap}. Original input is retained in
\code{original_data}; \code{embedding_metadata} holds feature order, weights,
per-period centers/scales, and fits. \code{diagnostics$embedding} is a tidy
spectrum/reconstruction table. Call \code{\link[=align_snapshots]{align_snapshots()}} before
\code{\link[=measure_drift]{measure_drift()}}. Supplied nonfeature metadata, such as cluster labels,
remains in \code{coordinates} for \code{\link[=match_clusters]{match_clusters()}}.
}
\description{
Embed repeated feature data or dissimilarities in two dimensions
}
\details{
Each period needs at least three entities and effective rank two.
Feature schemas are common across periods; missing/nonfinite measurements
are rejected without imputation. Active constant features cannot be
standardized. Inactive features receive scale one. Reserved coordinate
names \code{x} and \code{y} may be features, but cannot be extra metadata.

For features, the fitted geometry is the centered matrix with column j
divided by its declared sample SD and multiplied by \code{sqrt(weight[j])}. The
PCA \code{prcomp} model is fitted to the scaled, weighted input, not directly to
raw features. Its scores and loadings, raw centers, and scale/weight vectors
are retained. \code{predict(model, raw_data)} is therefore not a raw-data adapter.
Both methods store Gram eigenvalues (squared singular values for PCA).
PCA retains the complete SVD spectrum, omitting implicit zero Gram
eigenvalues when there are fewer features than entities. PCA component
variances additionally remain in \code{model$sdev^2}.

Distance input needs unique, nonblank entity labels, identical row/column
order, finite nonnegative symmetric entries, and an exactly zero diagonal.
Feature preprocessing arguments are unavailable for this input type.
MDS diagnostics report material negative eigenvalue counts, negative
absolute-inertia fraction (including roundoff), two-dimensional positive
and absolute inertia fractions, relative eigenvalue gap, and pairwise
distance RMSE. PCA has no negative spectrum and distance RMSE is \code{NA}.
A tie within the retained plane permits arbitrary rotation; a tie at its
boundary makes the selected plane ambiguous. Alignment cannot repair
truncation distortion or recover discarded dimensions.
}
\examples{
first <- data.frame(entity = letters[1:5], time = 1,
                    a = c(0, 3, 0, 2, 1), b = c(0, 0, 3, 2, 1))
second <- first
second$time <- 2
second$a[5] <- second$a[5] + 0.8
second$b[5] <- second$b[5] + 0.6
fit <- embed_snapshots(rbind(first, second), features = c("a", "b")) |>
  align_snapshots(anchors = letters[1:4])
measure_drift(fit)
fit$diagnostics$embedding
}
\seealso{
\code{\link[stats:prcomp]{stats::prcomp()}}, \code{\link[stats:cmdscale]{stats::cmdscale()}}, \code{\link[=align_snapshots]{align_snapshots()}}
}
