% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_drift_map}
\alias{plot_drift_map}
\alias{plot.driftmap}
\title{Plot repeated positions and adjacent-period trajectories}
\usage{
plot_drift_map(object, aligned = TRUE, trajectories = TRUE, labels = FALSE)

\method{plot}{driftmap}(x, ...)
}
\arguments{
\item{object}{A \code{driftmap} object from \code{\link[=drift_data]{drift_data()}}.}

\item{aligned}{Logical; use aligned coordinates. The object must first have
been processed by \code{\link[=align_snapshots]{align_snapshots()}} when \code{TRUE}.}

\item{trajectories}{Logical; draw arrows between adjacent observations.}

\item{labels}{Logical; label entities at their latest observed positions.
Overlapping text may be omitted. Dense plots may require a larger plotting
device or selective annotation.}

\item{x}{A \code{driftmap} object.}

\item{...}{Arguments passed to \code{\link[=plot_drift_map]{plot_drift_map()}}.}
}
\value{
A \code{ggplot} object that can be customized with ggplot2.
}
\description{
Display supplied or aligned two-dimensional coordinates. Lines and arrows
connect observations only when an entity is present in both immediately
adjacent scheduled periods. They never bridge an unobserved period.
}
\details{
Colors and point shapes distinguish periods. Equal coordinate-axis
scaling preserves angles and relative Euclidean distances. The plot does
not fit transformations or estimate uncertainty. Arrows show geometric
displacement and are not evidence of statistically significant movement.

With \code{aligned = FALSE}, raw arrows remain available to diagnose coordinate
artifacts and are labeled as unaligned. Set \code{trajectories = FALSE} to show
positions alone.
}
\examples{
first <- data.frame(entity = letters[1:4], time = 1,
                    x = c(0, 2, 0, 2), y = c(0, 0, 1, 1))
second <- transform(first, time = 2, x = -y + 3, y = x - 2)
map <- drift_data(rbind(first, second))
map <- align_snapshots(map)
plot_drift_map(map)
plot_drift_map(map, aligned = FALSE, trajectories = FALSE)
}
